export const threeDChromeBadgePrompt = (
  description: string,
  title: string,
  slogan: string,
  backgroundColor: string
) => {
  return `Highly detailed 3D rendering of the ${description}attached to the vertical product card, ultra smooth chrome finish, smooth and rounded 3D icons, programmatic futuristic design, soft reflections, clean shadows, paper card with a die cut euro hole at the top center, bold title '${title}' above the pin, interesting slogan '${slogan}' below, soft ${backgroundColor} background, soft studio lighting, minimalist aesthetics`;
};
