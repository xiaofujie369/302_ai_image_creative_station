export const plasticTrashBagPrompt = (
  bagColor: string,
  logoColor: string,
  centerLogo: string
) => {
  return `
A high-quality photograph of a full, shiny ${bagColor} plastic garbage bag with a smooth, crinkled texture. The bag is tied tightly at the top with a visible knot. Centered on the front of the bag is a clearly defined ${logoColor} ${centerLogo} logo, printed with sharp edges and high contrast. The background is a plain, neutral gray, and the lighting is soft and diffused, emphasizing the gloss of the plastic and the shape of the logo.`;
};
