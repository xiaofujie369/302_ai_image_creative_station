export const retroScienceFictionBookCoverPrompt = (
  subject: string,
  color1: string,
  color2: string
) => {
  return `
A retro science fiction book cover featuring ${subject}, drawn in a pulp style with granular textures, dramatic lighting, and saturated ${color1} and ${color2} tones. The worn edges, halftone dots, and bold retro layout form the framework of the scene.`;
};
