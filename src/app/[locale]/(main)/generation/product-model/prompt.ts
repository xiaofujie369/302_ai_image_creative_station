export const productModelPrompt = (modelRace: string, modelGender: string) => {
  return `
 A model of ${modelRace} ${modelGender} holds this product in their hand, with the color tone and lighting remaining unchanged.
  `;
};
